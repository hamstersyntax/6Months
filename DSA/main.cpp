#include <iostream>
#include <cmath>
using namespace std;
int power(int n, int b)
{
    int pow;
    for (int i = 0; i < b; i++)
    {
        pow = n * b;
    }
    return pow;
}
int main()
{
    int num = 5;
    int rem, decNum;
    // while (num > 0)
    // {
    //     rem = num % 2;
    //     decNum += 2 * 10;
    // }

    return 0;
}